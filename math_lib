#ifndef TESTES_MATH_H
#define TESTES_MATH_H
#endif

#include "libs.h"

bool prime(unsigned n) {
 if (n > 1) {
  for (unsigned i = 2; i < n-1; ++i)
   if (!(n % i))
    return FALSE;
  return TRUE;
 }
 return FALSE;
}
