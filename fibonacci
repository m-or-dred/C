#include <cstdio>

void fib(int n) {
    scanf("%u", &n);
    if (n > 0) {
        int a = 1, b = 1, i = 2;
        for (; i < n; i++) {
            int c = a + b;
            a = b;
            b = c;
        }
        printf("%d", b);
    }
    else printf("");
}
